<?xml version="1.0" encoding="UTF-8"?>

<configuration status="debug" monitorInterval="30">
	<Properties>
		<Property name="logPattern">[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%class:%L] - %msg%n</Property>
		<Property name="datePattern">%d{yyyy-MM-dd}</Property>
	</Properties>

	<appenders>
		<Console name="console" target="SYSTEM_OUT">
			<ThresholdFilter level="debug" onMatch="ACCEPT" onMismatch="ACCEPT" />
			<PatternLayout pattern="${logPattern}" />
		</Console>

		<RollingFile name="allLog" fileName="logs/all.log" filePattern="logs/all-${datePattern}.log.gz">
			<ThresholdFilter level="debug" onMatch="ACCEPT" onMismatch="ACCEPT" />
			<PatternLayout pattern="${logPattern}" />
			<!-- 设置策略 -->
			<Policies>
				<!-- 基于时间的触发策略。该策略主要是完成周期性的log文件封存工作。有两个参数： interval，integer型，指定两次封存动作之间的时间间隔。单位:以日志的命名精度来确定单位， 比如yyyy-MM-dd-HH 单位为小时，yyyy-MM-dd-HH-mm 
					单位为分钟 modulate，boolean型，说明是否对封存时间进行调制。若modulate=true， 则封存时间将以0点为边界进行偏移计算。比如，modulate=true，interval=4hours， 那么假设上次封存日志的时间为03:00，则下次封存日志的时间为04:00， 
					之后的封存时间依次为08:00，12:00，16:00 -->
				<TimeBasedTriggeringPolicy interval="1" modulate="true" />
			</Policies>
			<SizeBasedTriggeringPolicy size="500MB" />
			<DefaultRolloverStrategy max="10" />
		</RollingFile>

		<RollingFile name="errorLog" fileName="logs/error.log" filePattern="logs/error-${datePattern}.log.gz">
			<ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY" />
			<PatternLayout pattern="${logPattern}" />
			<!-- 设置策略 -->
			<Policies>
				<!-- 基于时间的触发策略。该策略主要是完成周期性的log文件封存工作。有两个参数： interval，integer型，指定两次封存动作之间的时间间隔。单位:以日志的命名精度来确定单位， 比如yyyy-MM-dd-HH 单位为小时，yyyy-MM-dd-HH-mm 
					单位为分钟 modulate，boolean型，说明是否对封存时间进行调制。若modulate=true， 则封存时间将以0点为边界进行偏移计算。比如，modulate=true，interval=4hours， 那么假设上次封存日志的时间为03:00，则下次封存日志的时间为04:00， 
					之后的封存时间依次为08:00，12:00，16:00 -->
				<TimeBasedTriggeringPolicy interval="1" modulate="true" />
			</Policies>
			<SizeBasedTriggeringPolicy size="500MB" />
			<DefaultRolloverStrategy max="10" />
		</RollingFile>
	</appenders>

	<loggers>
		<logger name="org.mybatis" level="info" />
		<logger name="org.apache" level="warn" />
		<logger name="org.apache.ibatis" level="debug" />
		<logger name="org.apache.mina" level="warn" />
		<logger name="httpclient.wire" level="warn" /><!-- 用于过滤org.apache.common.httpclient.wire -->
		<logger name="org.springframework" level="info" />
		<logger name="org.springframework.web" level="info" />
		<logger name="com.alibaba" level="info" />
		<logger name="springfox" level="info" />
		
		<root level="debug">
			<appender-ref ref="console" />
			<appender-ref ref="allLog" />
			<appender-ref ref="errorLog" />
		</root>
	</loggers>
</configuration>